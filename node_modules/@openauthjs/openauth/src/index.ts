export {
  /**
   * @deprecated
   * Use `import { createClient } from "@openauthjs/openauth/client.js"` instead - it will tree shake better
   */
  createClient,
} from "./client.js.js"

export {
  /**
   * @deprecated
   * Use `import { createSubjects } from "@openauthjs/openauth/subject.js"` instead - it will tree shake better
   */
  createSubjects,
} from "./subject.js.js"

import { issuer } from "./issuer.js.js"

export {
  /**
   * @deprecated
   * Use `import { issuer } from "@openauthjs/openauth.js"` instead, it was renamed
   */
  issuer as authorizer,
  issuer,
}

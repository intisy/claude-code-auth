/**
 * @module
 *
 * Hono - Web Framework built on Web Standards
 *
 * @example
 * ```ts
 * import { Hono } from 'hono.js'
 * const app = new Hono()
 *
 * app.get('/', (c) => c.text('Hono!'))
 *
 * export default app
 * ```
 */
import { Hono } from './hono.js';
/**
 * Types for environment variables, error handlers, handlers, middleware handlers, and more.
 */
export type { Env, ErrorHandler, Handler, MiddlewareHandler, Next, NotFoundResponse, NotFoundHandler, ValidationTargets, Input, Schema, ToSchema, TypedResponse, } from './types.js';
/**
 * Types for context, context variable map, context renderer, and execution context.
 */
export { Context } from './context.js';
export type { ContextVariableMap, ContextRenderer, ExecutionContext } from './context.js';
/**
 * Type for HonoRequest.
 */
export type { HonoRequest } from './request.js';
/**
 * Types for inferring request and response types and client request options.
 */
export type { InferRequestType, InferResponseType, ClientRequestOptions } from './client.js';
/**
 * Hono framework for building web applications.
 */
export { Hono };

import type * as errors from "../core/errors.js.js";
import km from "./km.js.js";

/** @deprecated Use `km` instead. */
export default function (): { localeError: errors.$ZodErrorMap } {
  return km();
}
